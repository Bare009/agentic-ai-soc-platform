# Dashboard provisioner — loads JSON dashboards from this directory on startup.
apiVersion: 1

providers:
  - name: SOC Platform
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
